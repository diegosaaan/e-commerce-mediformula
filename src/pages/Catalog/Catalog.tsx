import React, { ReactElement } from 'react';

const CatalogPage = (): ReactElement => (
  <div className="catalog-page">
    <h1 style={{ textAlign: 'center' }}>Страница каталога (в разработке)</h1>;
  </div>
);

export default CatalogPage;
